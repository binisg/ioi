 // if (vec[i] - vec[i-1] > r*2) return false;
    // else if (i == vec.size()-1 && vec[i] - vec[i-1] > r) return false;
    // else if (vec[i]-vec[i-1] == r) {
    //     if (vec[i+1]-vec[i] > r) return false;
    // }
    // i++;